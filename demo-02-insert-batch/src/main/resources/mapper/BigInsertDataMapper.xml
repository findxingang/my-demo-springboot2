<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.xingang.demo.mapper.BigInsertDataMapper">

    <insert id="insert" parameterType="pers.xingang.demo.domain.BigInsertData">
        insert into t_big_data_insert (id, name, sex, tel, email, road) values (#{id},#{name},#{sex},#{tel},#{email},#{road});
    </insert>
</mapper>